import React from "react";
import cn from "classnames";

export default class Music extends React.PureComponent {
  render() {
    const rootClassName = cn("c-music");

    return (
      <aside className={rootClassName}>
        {
          // Music Module
        }
      </aside>
    );
  }
}
